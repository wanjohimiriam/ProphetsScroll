<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/feature_card_background"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="12dp"
    android:paddingBottom="32dp">

    <!-- Drag handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@color/feature_card_border"
        android:backgroundTint="@color/feature_card_border" />

    <!-- Lock icon -->
    <ImageView
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="14dp"
        android:src="@drawable/ic_lock"
        app:tint="@color/gold_primary" />

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:fontFamily="sans-serif-medium"
        android:text="Sign in to continue"
        android:textColor="@color/feature_text_title"
        android:textSize="20sp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tvGateMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="28dp"
        android:gravity="center"
        android:text="Create a free account to like, comment, and save devotionals"
        android:textColor="@color/onboarding_subtitle"
        android:textSize="14sp"
        android:lineSpacingExtra="2dp" />

    <!-- Create Account -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGateSignUp"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:backgroundTint="@color/gold_primary"
        android:fontFamily="sans-serif-medium"
        android:text="Create free account"
        android:textAllCaps="false"
        android:textColor="@color/text_white"
        android:textSize="15sp"
        app:cornerRadius="14dp" />

    <!-- Sign In -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGateSignIn"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="10dp"
        android:backgroundTint="@android:color/transparent"
        android:fontFamily="sans-serif-medium"
        android:text="Sign In"
        android:textAllCaps="false"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        app:cornerRadius="14dp"
        app:strokeColor="@color/feature_card_border"
        app:strokeWidth="1dp" />

    <!-- Continue browsing -->
    <TextView
        android:id="@+id/tvContinueBrowsing"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="14dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:padding="6dp"
        android:text="Continue browsing"
        android:textColor="@color/onboarding_subtitle"
        android:textSize="14sp" />

</LinearLayout>