<?xml version="1.0" encoding="utf-8"?>
<!--
    Single verse row inside activity_bible layoutVerses container.
    Verse number inline with text — classic Bible layout.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:orientation="horizontal"
    android:gravity="top">

    <!-- Verse number -->
    <TextView
        android:id="@+id/tvVerseNumber"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:textColor="@color/gold_primary"
        android:textSize="10sp"
        android:paddingTop="3dp"
        tools:text="1" />

    <!-- Verse text -->
    <TextView
        android:id="@+id/tvVerseText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="sans-serif-light"
        android:lineSpacingExtra="5dp"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        tools:text="In the beginning God created the heavens and the earth." />

</LinearLayout>